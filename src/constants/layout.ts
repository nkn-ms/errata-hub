// ページの枠（左端の位置と最大幅）。ヘッダーも本文もこれを使う＝どの画面でも枠が動かない。
//
// なぜ1つに揃えるか: 以前はページごとに 1536 / 1024 / 768 / 672 と別々で、ページを移動すると
// 本文とヘッダーの左端が左右に動いていた（画面ごとにサイトが引っ越して見える）。
//
// なぜ上限を 1024（screen-lg）で止めるか＝**行長**。本文は 14px なので、
// 1024px なら1行あたり約73文字で可読性の推奨（45〜75文字）に収まる。
// 1536px にすると約110文字になり、長文（利用規約・使い方）が読みづらくなる。
// ⚠️ これは上限なので、1024px 未満の画面では画面幅に追従する（sm・md では全幅）。
//
// 検索テーブル（/reports）が収まるかは実測済み: 枠 1024 のとき表の必要幅 958px = はみ出し 0px。
export const PAGE_CONTAINER = "max-w-screen-lg mx-auto px-4 sm:px-6 lg:px-8";

// 入力・設定系（/submit・/account）で、枠の内側にさらに置く入力カラム。
// 理由: 入力欄や「ラベル—値」の行が 1024px いっぱいに伸びると視線移動が長く、対応関係も追いにくい。
// 枠（PAGE_CONTAINER）の左端は他のページと共通なので、ページ間で位置は動かない。
export const FORM_COLUMN = "max-w-2xl";
